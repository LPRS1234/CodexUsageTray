# CodexUsageTray 작업 지침

## 프로젝트 개요

- Windows 전용 로컬 트레이 애플리케이션이다.
- .NET Framework 4.8의 C# 컴파일러와 Windows Forms를 사용한다.
- 프로젝트 파일이나 별도 패키지 관리 도구 없이 `build.cmd`로 빌드한다.
- `assets/`에는 로컬 대시보드의 HTML, CSS, JavaScript와 이미지가 있다.
- `bin/`은 생성 결과물이므로 직접 수정하거나 커밋하지 않는다.

## 작업 원칙

- 수정 전에 관련 소스 파일, 테스트, 기존 구현 사례를 확인한다.
- 요청과 직접 관련된 파일만 수정한다.
- 관련 없는 코드 정리, 리팩터링, 이름 변경 또는 대규모 포맷 변경을 하지 않는다.
- 기존 코드의 네임스페이스, 중괄호, 명명 및 오류 처리 방식을 따른다.
- 설치된 .NET Framework 컴파일러가 지원하지 않을 수 있는 최신 C# 문법을 사용하지 않는다.
- 새로운 외부 라이브러리나 빌드 도구를 추가하기 전에 사용자에게 확인한다.

## 주요 디렉터리

- `src/App`: 애플리케이션 흐름, 설정, 자동 실행 및 사용량 갱신
- `src/Codex`: Codex App Server 통신과 응답 모델
- `src/Dashboard`: 로컬 대시보드 서버와 상태
- `src/UI`: 트레이 카드와 Windows Forms UI
- `src/Interop`: Windows API 연동
- `assets`: 대시보드 정적 파일
- `tests`: 스모크 테스트

## 보안 및 개인정보

- 인증 토큰, API 키, 비밀번호 또는 개인정보를 출력하거나 저장하지 않는다.
- Codex 인증은 기존 Codex CLI 로그인과 로컬 stdio 연결을 사용한다.
- 대시보드 서버는 `127.0.0.1` 전용 상태를 유지한다.
- 계정 이메일, 사용량 수치 또는 인증 정보를 테스트 출력과 로그에 추가하지 않는다.
- 인증 방식이나 네트워크 노출 범위를 변경해야 한다면 구현 전에 사용자에게 확인한다.

## 빌드와 검증

- 빌드: `.\build.cmd`
- 테스트: `.\test.cmd`
- 수동 실행: `.\run.cmd`

C# 소스를 변경한 후에는 가능한 경우 다음을 실행한다.

1. `.\build.cmd`
2. `.\test.cmd`

대시보드의 HTML, CSS 또는 JavaScript를 변경한 경우에도 빌드와 테스트를 실행하고,
시각적 동작을 직접 확인하지 못했다면 완료 보고에 그 사실을 명시한다.

검증이 실패하면 실패를 숨기지 말고 실행한 명령, 오류 원인 및 남은 문제를 보고한다.

## 완료 보고

완료 보고에는 다음을 간결하게 포함한다.

- 변경한 내용
- 실행한 검증과 결과
- 확인하지 못한 항목이나 남은 위험
